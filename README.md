# Arveno Content

Rendered marketing assets (carousel slides + captions) for Arveno Fitness — public so social schedulers (Buffer) can fetch them by URL. No code, no secrets, ever.

Generated by the carousel factory in the main repo (`marketing/carousel-factory/`).
Layout: `carousels/<slug>/slide-NN.png` + `caption.txt`.
